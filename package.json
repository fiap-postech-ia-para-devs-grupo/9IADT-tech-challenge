{
  "name": "tech-challenge-fase2",
  "version": "0.1.0",
  "scripts": {
    "api": "uv run uvicorn tech_challenge.adapters.api:app --reload --port 8000",
    "app": "uv run streamlit run src/tech_challenge/presentation/streamlit_app.py",
    "install": "uv sync --frozen",
    "dev": "concurrently \"npm run api\" \"npm run app\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
